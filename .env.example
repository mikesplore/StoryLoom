# AI Provider Configuration
# StoryLoom supports multiple AI providers with automatic fallback
# Configure at least ONE of the following API keys:

# Google Gemini API (Primary - Recommended)
# Get your free API key at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Hugging Face API (Fallback - Free Tier Available)
# Used for both story generation (fallback) and cover image generation
# Get your free token at: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_token_here

# Note: If Gemini is unavailable, the system automatically uses Hugging Face
# For best results, configure both API keys

# Flask Secret Key
SECRET_KEY=your-super-secret-key-here